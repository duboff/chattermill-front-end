import {
  titleizeText
} from 'chattermill-front/helpers/titleize-text';

module('TitleizeTextHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = titleizeText(42);
  ok(result);
});
